[project]
name = "airfoil-ml"
version = "0.1.0"
description = "Airfoil aerodynamics project for learning statistical and machine learning methods"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]